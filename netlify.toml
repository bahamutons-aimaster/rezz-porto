[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# Biar /admin juga bisa diakses tanpa ".html"
[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200
